---
import { Button } from "../components/Buttons";
import Layout from "../components/Layout.astro";
---

<Layout title={undefined} description={undefined}>
  <div class="mt-60 h-full w-full flex-1 flex items-center justify-center">
    <div class="border border-primary2 rounded-2xl p-8 flex flex-col gap-6 items-center">
      <h1 class="font-secondary text-5xl font-bold">
        {Astro.locals.t.pageNotFound.title}
      </h1>
      <Button label={Astro.locals.t.pageNotFound.button} href={'/'} />
    </div>
  </div>
</Layout>
