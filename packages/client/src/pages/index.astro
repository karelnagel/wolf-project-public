---
import { getLocaleUrl } from "@template/shared/helpers";

export const prerender = false;
const url = getLocaleUrl(Astro.locals.locale);
if (url === "idkwhyastrothinksthaturlisnotused") return;
return Astro.redirect(url);
---
