---
import { ClientInfo } from "../../../components/ClientInfo";
import Layout from "../../../components/Layout.astro";
const { projectId } = Astro.params;
if (!projectId) throw new Error("id is required");
console.log("ID:", projectId);
const clients = [
  { name: "Maru1", email: "maru1@veebihunt.ee" },
  //   { name: "Maru2", email: "maru2@veebihunt.ee" },
  //   { name: "Maru3", email: "maru3@veebihunt.ee" },
  //   { name: "Maru4", email: "maru4@veebihunt.ee" }
];
---

<Layout title="uugabuuga" description="uugabuuga">
  <div class="flex justify-center items-center px-20 py-12 font-semibold max-md:px-5">
    <div
      class="flex flex-col items-center px-0 py-10 rounded-2xl border border-primary2 border-solid w-1/3 max-md:px-5 max-w-md max-md:mt-10"
    >
      <div class="text-xl text-center">Kliendi info:</div>
      <textarea
        placeholder="Ettevõtte nimi"
        class="justify-center w-2/3 px-2.5 py-3 mt-8 h-12 text-base rounded-2xl bg-primary text-opacity-50"
      ></textarea>
      <ClientInfo clients={clients} client:load />
      <div
        class="flex flex-wrap gap-5 justify-center mt-8 text-base font-extrabold max-w-md whitespace-nowrap"
      >
        <button
          class="justify-center px-12 py-2.5 rounded-2xl border-solid border-[3px] border-primary max-md:px-5"
        >
          Tagasi
        </button>
        <button class="justify-center px-12 py-2.5 rounded-2xl bg-primary2 max-md:px-5">
          Edasi
        </button>
      </div>
    </div>
  </div>
</Layout>
